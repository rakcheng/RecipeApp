# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RecipeApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecipeApp
  # This pulls in the latest versions of Alamofire. IF you have an older version of Xcode, please use 4.0 or check their documentation!
  pod 'AlamofireImage'
  pod 'Parse'
end
